Drop the product walkthrough video here as:
  oriant-product-demo.mp4
Optional poster:
  product-demo-poster.jpg
The landing page picks either up automatically (a designed placeholder shows until then).
The older /public/videos location also still works as a fallback.
