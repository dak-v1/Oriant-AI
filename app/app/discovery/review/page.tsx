import DiscoveryWorkspace from "@/components/mock/discovery/DiscoveryWorkspace";

/** Discovery Review runs the final evidence check before report generation. */
export default function DiscoveryReviewPage() {
  return <DiscoveryWorkspace />;
}
