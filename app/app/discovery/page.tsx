"use client";
/**
 * /app/discovery — Discovery Agent voice interview + live "What Oriant knows"
 * (spec §9). Thin route file; the experience lives in components/mock/discovery.
 */
import DiscoveryWorkspace from "@/components/mock/discovery/DiscoveryWorkspace";

export default function DiscoveryPage() {
  return <DiscoveryWorkspace />;
}
