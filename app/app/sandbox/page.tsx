"use client";
/**
 * /app/sandbox — Sandbox + stress test (spec §15). Thin route file; the
 * experience lives in components/mock/sandbox/.
 */
import SandboxScreen from "@/components/mock/sandbox/SandboxScreen";

export default function SandboxPage() {
  return <SandboxScreen />;
}
