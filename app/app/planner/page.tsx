"use client";
/**
 * /app/planner — AI Workforce Planner + Human Approval Gate 2 (spec §11, §13).
 * Thin route: the whole experience lives in components/mock/planner/.
 */
import PlannerExperience from "@/components/mock/planner/PlannerExperience";

export default function PlannerPage() {
  return <PlannerExperience />;
}
